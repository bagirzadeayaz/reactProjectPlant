# features

User actions that change something: add to cart, toggle wishlist, submit the newsletter
form, switch language. A feature owns its interaction, its local state and its side
effects.

**May import:** entities, shared.
**May be imported by:** widgets, pages, app.

Name a feature after the verb, not the noun — `add-to-cart`, not `cart`.
