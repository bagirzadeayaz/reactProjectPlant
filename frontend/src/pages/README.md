# pages

One folder per route. A page composes widgets and features into a screen and owns the
route's data requirements — nothing more.

**May import:** widgets, features, entities, shared.
**May be imported by:** app.

A page should read like a table of contents. If a page file is growing logic, that logic
wants to be a feature or a widget.
